1. Git 相关素材

准备几个例子即可，不一定是真实数据。

Commit    修改文件    影响模块
c1a2b3    OrderService.java    订单模块
d4e5f6    PaymentCallbackController.java    支付模块
a7b8c9    InventoryService.java    库存模块

可以在 PPT 里写：

通过 Commit 记录抽取“开发人员—Commit—文件—模块”的关系。

2. Issue / Bug 相关素材

准备 3~5 个模拟 Bug。

Issue    Bug 描述    影响模块    修复人员
#101    支付成功后订单状态未更新    订单模块、支付模块    张三
#102    库存扣减失败导致下单失败    库存模块、订单模块    李四
#103    订单取消后优惠券未返还    订单模块、优惠模块    王五
#104    物流状态同步延迟    物流模块、订单模块    赵六

用于回答：

最近 30 天 Bug 最多的模块？
谁修复过支付相关 Bug 最多？

3. 日志相关素材

准备几个 LogEvent 示例：

LogEvent    异常类型    TraceId    关联模块
L001    PaymentCallbackTimeout    T001    支付模块
L002    InventoryLockFailed    T002    库存模块
L003    OrderStatusUpdateFailed    T003    订单模块
L004    CouponRollbackFailed    T004    优惠模块

对应关系：

LogEvent → 异常类型聚类
LogEvent → TraceId
TraceId → 订单
LogEvent → 系统模块

4. API 文档相关素材

准备接口和模块映射：

API 接口    所属模块    相关测试
POST /orders    订单模块    订单创建测试
POST /orders/{id}/pay    支付模块    支付流程测试
POST /payments/callback    支付模块    支付回调测试
POST /inventory/lock    库存模块    库存锁定测试
POST /orders/{id}/cancel    订单模块    订单取消测试
POST /refunds    售后模块    退款测试

这个表可以直接用于第 5 页“订单模块变更影响哪些测试”。

gpt建议找这些素材

然后把“模块”都换成“服务”吧，那个老师之前一直说微服务