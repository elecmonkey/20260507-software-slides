flowchart LR
    Git[Git 提交记录] --> GitExtract[Commit / 文件修改 / 模块变更]
    Issue[Issue / Bug] --> IssueExtract[Bug 描述 / 影响模块 / 修复人员]
    Log[运行日志] --> LogExtract[LogEvent / 异常类型 / TraceId]
    API[API 文档] --> APIExtract[接口 / 模块映射 / 测试用例]

    GitExtract --> KG[电商订单故障排查知识图谱]
    IssueExtract --> KG
    LogExtract --> KG
    APIExtract --> KG

    KG --> Q1[影响分析]
    KG --> Q2[故障定位]
    KG --> Q3[Bug 统计]
    KG --> Q4[回归测试范围]

mindmap
  root((电商订单故障排查图谱实体))
    业务实体
      用户
      订单
      商品
      SKU
      支付单
      库存记录
      物流单
      售后单
    系统模块实体
      订单模块
      支付模块
      库存模块
      物流模块
      售后模块
      优惠模块
    代码变更实体
      GitCommit
      代码文件
      开发人员
      模块变更
    故障日志实体
      Issue
      Bug
      LogEvent
      异常类型
      TraceId
    接口测试实体
      API接口
      API文档
      测试用例

flowchart TB
    subgraph Git数据源
        Dev[开发人员] -->|提交| Commit[Git Commit]
        Commit -->|修改| File[代码文件]
        File -->|属于| ModuleA[系统模块]
        Commit -->|影响| ModuleA
    end

    subgraph Issue数据源
        Issue[Issue] -->|描述| Bug[Bug]
        Bug -->|影响| ModuleB[系统模块]
        Dev2[修复人员] -->|修复| Bug
        Bug -->|关联| API1[API 接口]
    end

    subgraph 日志数据源
        LogEvent[LogEvent] -->|聚类为| Exception[异常类型]
        LogEvent -->|关联| TraceId[TraceId]
        TraceId -->|追踪| Order[订单]
        LogEvent -->|属于| ModuleC[系统模块]
    end

    subgraph API文档数据源
        API2[API 接口] -->|属于| ModuleD[系统模块]
        API2 -->|操作| Entity[业务实体]
        API2 -->|被覆盖| TestCase[测试用例]
    end

flowchart LR
    OrderModule[订单模块]:::core

    %% 业务链路
    User[用户] -->|创建| Order[订单]
    Order -->|生成| Payment[支付单]
    Order -->|占用| Inventory[库存记录]
    Order -->|生成| Logistics[物流单]
    Order -->|产生| AfterSale[售后单]
    Order -->|包含| Product[商品]
    Product -->|对应| SKU[SKU]

    OrderModule -->|管理| Order
    PaymentModule[支付模块] -->|处理| Payment
    InventoryModule[库存模块] -->|管理| Inventory
    LogisticsModule[物流模块] -->|管理| Logistics
    AfterSaleModule[售后模块] -->|处理| AfterSale

    %% 接口与测试
    API[API 接口] -->|属于| OrderModule
    API -->|被覆盖| TestCase[测试用例]

    %% Git 变更
    Dev[开发人员] -->|提交| Commit[Git Commit]
    Commit -->|修改| File[代码文件]
    File -->|属于| OrderModule
    Commit -->|影响| OrderModule

    %% Issue / Bug
    Issue[Issue] -->|描述| Bug[Bug]
    Bug -->|影响| OrderModule
    Bug -->|关联| API
    Dev -->|修复| Bug

    %% 日志链路
    LogEvent[LogEvent] -->|聚类为| Exception[异常类型]
    LogEvent -->|关联| TraceId[TraceId]
    TraceId -->|追踪| Order
    LogEvent -->|属于| OrderModule

    classDef core fill:#f6f6f6,stroke:#333,stroke-width:2px;

flowchart LR
    Commit[Git Commit] -->|修改| File[代码文件]
    File -->|属于| OrderModule[订单模块]
    OrderModule -->|提供| API[API 接口]
    API -->|被覆盖| TestCase[测试用例]

    TestCase --> TC1[订单创建测试]
    TestCase --> TC2[支付回调测试]
    TestCase --> TC3[库存锁定测试]
    TestCase --> TC4[订单取消测试]

flowchart LR
    Issue1[Issue #101] --> Bug1[支付成功订单未更新]
    Issue2[Issue #102] --> Bug2[库存扣减失败]
    Issue3[Issue #103] --> Bug3[订单取消失败]
    Issue4[Issue #104] --> Bug4[物流状态同步延迟]
    Issue5[Issue #105] --> Bug5[退款回调失败]

    Bug1 -->|影响| OrderModule[订单模块]
    Bug1 -->|影响| PaymentModule[支付模块]
    Bug2 -->|影响| InventoryModule[库存模块]
    Bug2 -->|影响| OrderModule
    Bug3 -->|影响| OrderModule
    Bug4 -->|影响| LogisticsModule[物流模块]
    Bug5 -->|影响| AfterSaleModule[售后模块]
    Bug5 -->|影响| PaymentModule

flowchart LR
    PaymentModule[支付模块] -->|相关| Bug1[支付回调超时]
    PaymentModule -->|相关| Bug2[支付成功订单未更新]
    PaymentModule -->|相关| Bug3[退款回调失败]
    PaymentModule -->|相关| Bug4[支付单重复创建]

    DevA[张三] -->|修复| Bug1
    DevA -->|修复| Bug2
    DevB[李四] -->|修复| Bug3
    DevA -->|修复| Bug4

flowchart LR
    Commit[代码提交] --> Extract[抽取变更信息]
    Extract --> UpdateKG[更新知识图谱]
    UpdateKG --> Analyze[影响分析]
    Analyze --> Test[选择回归测试]
    Test --> Fix[修复与上线]
    Fix --> IssueClose[关闭 Issue]
    IssueClose --> Sync[同步 Bug / 修复人员 / Commit 关系]
    Sync --> UpdateKG

flowchart LR
    LogEvent[LogEvent: OrderStatusUpdateFailed] -->|聚类为| Exception[订单状态更新异常]
    LogEvent -->|关联| TraceId[TraceId: T20260507001]
    TraceId -->|追踪| API[支付回调接口]
    API -->|属于| PaymentModule[支付模块]
    TraceId -->|追踪| Order[订单: ORD20260507001]
    Order -->|当前状态| Status[未支付]
    Order -->|生成| Payment[支付单]
    Payment -->|支付状态| PayStatus[已支付]

flowchart TB
    Risk[图谱维护风险] --> R1[数据源分散]
    Risk --> R2[图谱更新不及时]
    Risk --> R3[模块映射不准确]
    Risk --> R4[日志缺少 TraceId]
    Risk --> R5[Issue 描述不规范]

    R1 --> S1[统一抽取规则]
    R2 --> S2[随版本自动同步]
    R3 --> S3[核心关系人工校验]
    R4 --> S4[统一日志规范]
    R5 --> S5[制定 Issue 模板]

请你挑几个mermaid放进PPT，不要全放！